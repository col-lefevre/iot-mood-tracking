import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "flex-start",
        alignItems: "center",
    },
    flatList: {
        marginHorizontal: 20,
        flexGrow: 0,
        marginVertical: 10,
    },
});

export { globalStyles };
